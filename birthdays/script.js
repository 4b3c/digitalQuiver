
function setup() {
  createCanvas(windowWidth, 400);
}

function draw() {
    background(89, 213, 247);
    fill(255, 10, 10);
    textAlign(CENTER);
    textSize(50);
    text("Happy Birthday Campbell!!", windowWidth * 0.5, 40);
    ellipse(windowWidth * 0.1, 200, 80, 120);
    ellipse(windowWidth * 0.3, 200, 80, 120);
}
